
<section id="portada" class="bg-fixed w-full h-screen bg-no-repeat" style="background-image: url(<%= asset_path 'content/dalle-militar.png' %>);background-size: cover;">
  <div id="suscribe" class="suscribe w-full h-full flex flex-col justify-center items-center relative">
    <div class="w-full xl:w-2/5 flex flex-col justify-center items-center bg-emerald-950 p-14 border border-transparent rounded-xl">
      <% if current_user.admin? %>
        <%= form_with(model: @info, class: 'bg-transparent opacity-100') do |form|%>
          <%= form.text_field :main_title, value: @info.secondary_title, class: 'w-full text-5xl font-bold text-emerald-400 text-center' %>
          <%= form.text_field :main_subtitle, value: @info.secondary_subtitle, class: 'w-full text-3xl font-bold text-emerald-400 text-center' %>
          <%= form.submit "Actualizar", class: 'text-white bg-blue-500 rounded-xl px-4 py-2' %>
        <% end %>
      <% else %>
        <h1 class="text-5xl font-bold text-emerald-400 text-center"><%= @info.main_title%></h1>
        <h2 class="text-3xl font-bold text-emerald-400 text-center"><%= @info.main_subtitle%></h2>
      <% end %>
      <div class="w-full mt-8 flex flex-col xl:flex-row justify-center items-center gap-12">

          <%= link_to '#', class: " flex justify-start items-start gap-8 hover:scale-125 transition-all ease-in-out duration-500" do %>
            <%= image_tag('content/military-right.png', class: 'w-32') %>
            <span class="w-fit h-12 mt-4 py-2 px-8 border border-blue-600 hover:bg-blue-600 text-blue-600 hover:text-white rounded-xl">Suscribe</span>
          <% end %>

          <%= link_to '#nosotros', class: " flex justify-start items-start gap-8 hover:scale-125 transition-all ease-in-out duration-500" do %>
            <span class="w-fit h-12 mt-4 py-2 px-8 border border-blue-600 hover:bg-blue-600 text-blue-600 hover:text-white rounded-xl">Saber más</span>
            <%= image_tag('content/military-right.png', class: 'w-32 transform scale-x-[-1]') %>
          <% end %>

      </div>
    </div>
  </div>
</section>
<!-- About Us Section -->
<section id="nosotros" class="container mx-auto p-12 text-center my-12">
    <h2 class="text-3xl font-bold text-slate-400">Sobre nosotros</h2>

    <% if current_user.admin? %>
      <%= form_with(model: @info, local: true, class: 'mt-4 text-green-200 text-lg p-8 bg-teal-700 rounded-xl shadow-teal-800') do |form| %>
        <%= form.text_area :about, value: @info.about, class: 'w-full min-h-24 bg-transparent' %>

        <%= form.submit "Actualizar", class: 'text-white bg-blue-500 rounded-xl px-4 py-2' %>

      <% end %>
    <% else %>
      <p class="mt-4 text-green-200 text-lg p-8 bg-teal-700 rounded-xl shadow-teal-800"><%= @info.about %></p>
    <% end %>
    
</section>

<!-- Programs Section -->
<%= turbo_frame_tag :cards, src: cards_path, load: :lazy %>

<section id="portada" class="bg-fixed w-full h-screen bg-no-repeat" style="background-image: url(<%= asset_path 'content/dalle-militar-wait.png' %>);background-size: cover;">
  <div id="info" class="suscribe w-full h-full flex flex-col justify-center items-center relative">
    <div class="w-full xl:w-2/5 flex flex-col justify-center items-center bg-emerald-950 p-14 border border-transparent rounded-xl">
      
      <% if current_user.admin? %>
        <%= form_with(model: @info, class: 'bg-transparent opacity-100') do |form|%>
          <%= form.text_field :secondary_title, value: @info.secondary_title, class: 'w-full text-5xl font-bold text-emerald-400 text-center' %>
          <%= form.text_field :secondary_subtitle, value: @info.secondary_subtitle, class: 'w-full text-3xl font-bold text-emerald-400 text-center' %>
          <%= form.submit "Actualizar", class: 'text-white bg-blue-500 rounded-xl px-4 py-2' %>
        <% end %>
      <% else %>
        <h1 class="text-5xl font-bold text-emerald-400 text-center"><%= @info.secondary_title%></h1>
        <h2 class="text-3xl font-bold text-emerald-400 text-center"><%= @info.secondary_subtitle%></h2>
      <% end %>
      <div class="w-full mt-8 flex flex-col xl:flex-row justify-center items-center gap-12">
          <%= link_to '#nosotros', class: " flex justify-start items-start gap-8 hover:scale-125 transition-all ease-in-out duration-500" do %>
            <%= image_tag('content/military-right.png', class: 'w-32') %>
            <span class="w-fit h-12 mt-4 py-2 px-8 border border-blue-600 hover:bg-blue-600 text-blue-600 hover:text-white rounded-xl">Saber más</span>
          <% end %>

          <%= link_to '#contacto', class: " flex justify-start items-start gap-8 hover:scale-125 transition-all ease-in-out duration-500" do %>
            <span class="w-fit h-12 mt-4 py-2 px-8 border border-blue-600 hover:bg-blue-600 text-blue-600 hover:text-white rounded-xl">Contacto</span>
            <%= image_tag('content/military-right.png', class: 'w-32 transform scale-x-[-1]') %>
          <% end %>
      </div>
    </div>
  </div>
</section>

<!-- Testimonials Section -->
<section id="testimonios" data-controller="swiper" class="container mx-auto relative flex flex-col gap-4  mt-12">
  <h2 class="text-3xl font-bold text-slate-400 text-center">Testimonios</h2>
  <!-- Tarjeta de Testimonio -->
  <div data-swiper-target="container" class="overflow-hidden w-5/6 mx-auto">
    <div class="swiper-wrapper h-full flex">
      <% @users.each do |user| %>
        <div class="swiper-slide px-4 flex-grow">
          <div class="flex items-center px-6 py-3 bg-gray-900 border border-transparent rounded-t-xl">
              <h1 class="mx-3 text-white font-semibold text-lg"> <%= user[:title] %></h1>
          </div>

          <div class="p-6 bg-emerald-950 border border-transparent rounded-b-xl shadowlg shadow-emerald-900">
            <div class="flex items-center">
              <img class="h-10 w-10 rounded-full" src="<%= user[:avatar] %>" alt="perfil">
              <div class="ml-3">
                <p class="text-green-200 text-lg font-semibold"><%= user[:author] %></p>

                <!-- Valoración con estrellas -->
                <div class="flex">
                  <% user[:stars].times do %>
                    <svg class="w-4 h-4 text-yellow-500" fill="currentColor" viewBox="0 0 24 24"><path d="M12 .587l3.668 7.429 8.332 1.214-6.026 5.884 1.426 8.31L12 18.897l-7.4 4.527 1.426-8.31-6.026-5.884 8.332-1.214z"/></svg>
                  <% end %>
                </div>
              </div>
            </div>

            <p class="mt-3 text-green-700 text-sm"><%= user[:comment] %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
  <!-- Add Navigation -->
  <div data-swiper-target="prev" class="swiper-button-next"></div>
  <div data-swiper-target="next" class="swiper-button-prev"></div>
</section>


<!-- Contact Section -->
<section id="contacto" class="p-12 text-center">
  <h2 class="text-3xl font-bold text-slate-400">Contáctanos</h2>
  <form class="mt-8">
      <div class="mx-auto max-w-2xl">
          <div class="py-2">
              <span class="px-1 text-sm text-gray-600">Nombre</span>
              <input placeholder="" type="text" class="text-md text-gray-300 block px-3 py-2 rounded-lg w-full bg-gray-900 border-2 border-transparent focus:outline-none focus:border-gray-600" />
          </div>
          <div class="py-2">
              <span class="px-1 text-sm text-gray-600">Email</span>
              <input placeholder="" type="text" class="text-md text-gray-300 block px-3 py-2 rounded-lg w-full bg-gray-900 border-2 border-transparent focus:outline-none focus:border-gray-600" />
          </div>
          <div class="py-2">
              <span class="px-1 text-sm text-gray-600">Mensaje</span>
              <textarea placeholder="" class="text-md block text-gray-300 px-3 py-2 rounded-lg w-full bg-gray-900 border-2 border-transparent focus:outline-none focus:border-gray-600 h-32"></textarea>
          </div>
          <div class="py-2">
              <button type="submit" class="bg-gray-900 text-gray-100 px-5 py-2 rounded-lg hover:bg-gray-800">Enviar</button>
          </div>
      </div>
  </form>
</section>




